[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spacetope"
version = "0.0.1"
description = "Assemble a list of spaces into verified topologicpy cell complexes with circulation"
requires-python = ">=3.11,<3.12"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
dependencies = [
  "topologicpy==0.9.71",
  "topologic_core==8.0.0",
  "networkx>=3.3",
  "ortools>=9.10",
  "shapely>=2.0",
  "scipy>=1.12",
  "numpy>=1.26",
  "pyyaml>=6",
  "plotly>=5",
  "trimesh>=4",
]

[project.optional-dependencies]
dev = ["pytest>=8", "squarify", "z3-solver>=4.12"]

[project.scripts]
spacetope = "spacetope.cli:main"

[tool.setuptools.packages.find]
include = ["spacetope*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "gate_m0: M0 gate", "gate_m1: M1 gate", "gate_m2: M2 gate", "gate_m3: M3 gate",
  "gate_m4: M4 gate", "gate_m5: M5 gate", "gate_m6: M6 gate", "gate_m7: M7 gate", "gate_m8: M8 gate",
  "gate_m9: M9 gate", "gate_m10: M10 gate", "gate_m11: M11 gate", "gate_m12: M12 gate", "gate_m13: M13 gate", "gate_m14: M14 gate", "gate_m15: M15 gate", "gate_m16: M16 gate",
  "slow: builds topologic geometry",
]
addopts = "-q"
